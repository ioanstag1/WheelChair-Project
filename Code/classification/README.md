# Classification

This folder contains:

- Nested Leave-One-Out cross-validation scripts
- Feature selection strategies (t-test, RFE, SVM weights)
- Model training (SVM, Logistic Regression, Random Forest)

